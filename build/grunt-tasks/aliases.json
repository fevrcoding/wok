{
	"renderview": ["render"],

	"preprocess:dev": ["compass:dev"],

	"preprocess:dist": ["compass:dist"],

	"dev": [
		"clean",
		"copy",
		"preprocess:dev",
		"renderview",
		"sassdown"
	],

	"dist": [
		"clean",
		"copy",
		"imagemin",
		"preprocess:dist",
		"renderview",
		"htmlrefs:dist",
		"useminPrepare",
		"concat",
		"uglify",
		"cssmin",
		"modernizr",
		"filerev",
		"filerev_assets",
		"usemin"
	],

	"serve": ["default:server"]
}